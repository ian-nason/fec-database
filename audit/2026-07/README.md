# July 2026 audit repairs (provenance only)

These scripts were run **in place** against the 2026-07-06 build of this database and
produced the numbers reported in `CHANGELOG.md`. Their logic has since been folded into
`build_database.py` (or its validation step), so a fresh rebuild reproduces the repaired
state without them.

Do not run them against a new build. They are kept so the changelog is auditable.
